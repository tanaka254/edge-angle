# 階段補強板 角度計算 — アプリ（PWA）配布キット

職人さん全員がスマホで「アプリのように」使える、勾配・突付け・留めの角度計算アプリです。
**アプリストア不要。リンクを開いて「ホーム画面に追加」するだけ**で、アイコン付き・全画面・オフライン動作になります。

## 中身
- `index.html` … アプリ本体（計算機）
- `manifest.json` … アプリ情報（名前・アイコン・全画面設定）
- `sw.js` … オフライン動作用（サービスワーカー）
- `icon-192.png` / `icon-512.png` / `icon-180.png` … アプリアイコン

## 公開する（どれか1つでOK）

### A. GitHub Pages（おすすめ・無料）
1. このフォルダの中身を GitHub リポジトリに入れる（新規 `edge-angle` か、`edge-kaisyu-app` の `docs/` フォルダなど）
2. GitHub → **Settings → Pages** → Branch を `main` / フォルダ（`/docs` か `/root`）に設定
3. 数分で `https://tanaka254.github.io/＜リポジトリ名＞/` に公開される

### B. Netlify（ドラッグ&ドロップ・無料）
1. netlify.com にログイン
2. このフォルダをそのまま画面にドラッグ&ドロップ
3. すぐ `https://＜自動名＞.netlify.app` で公開（好きな名前に変更可）

### C. 既存サイトに置く
`edge-corp.co.jp` のサブフォルダ（例 `/tool/kakudo/`）にアップロードでもOK。

> どの方法でも **https:// で配信**されることが条件（PWA・オフラインの要件）。GitHub Pages・Netlify は自動で https。

## 職人さんへの配布
1. 公開URLを **LINE WORKS** で共有（**QRコード**があると現場で読み取るだけで楽）
2. 各自スマホでURLを開く
3. **iPhone**：共有ボタン → 「ホーム画面に追加」 ／ **Android**：メニュー → 「アプリをインストール」
4. 以後はホーム画面のアイコンから、電波が無くても起動

## 更新のしかた
ファイルを差し替えて再アップ（GitHub なら push）すれば、次回起動時に全員へ自動反映。
確実に反映させたいときは `sw.js` の `CACHE` 名（`edge-kakudo-v1`）の数字を1つ上げる。

## 将来：edge-kaisyu-app へ統合
React/Vite 側に1画面（ルート）として組み込めば、見積・構成図メーカーと同じアプリ内に集約できます。
入力＝蹴上・踏面（＋蹴込み）、出力＝勾配θ・突付け・留め、回り階段は壁ごとに実測値、という仕様。
